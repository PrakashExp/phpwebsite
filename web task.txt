Back-end (maybe php/Node):
	1. Design and create database
	2. Create connection from php to mysql
	3. Create dummy data for database
	4. List of api need to be created:
		+ /login
		+ /createUser
		+ /forgotPassword
		+ /getAllFlowerItems
		+ /user/profile
		+ /user/updateProfile
		+ /user/addCart
		+ /user/getListCart
		+ /admin/addItems
		+ /admin/updateItems
		+ /admin/deletedItems
		+ /admin/getAllItems
		+ /admin/listingUsers
		+ /search
	
	
Front-end:
	0. How many pages should we use for this project: 
		Landing page -> show the currents item in store.
		Login page + Sign up -> login or sign up.
		Forgot password page -> recover password.
		Item details page -> show details of the item even the attributes.
		Cart page -> listing all of items want to buy.
		Checkout page -> check one more time the info and billing before
		                 proceeding transaction.
		Account page -> show details information of user and can be edited.
		Admin page -> manage user, billing, create new item for shop.
		Contact page -> show information of shop.
		
	1. Modify style of template suit with flower template (html+css)
	2. Angular JS control behaviour of pages.